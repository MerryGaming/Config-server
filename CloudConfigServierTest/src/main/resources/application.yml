spring.profiles.active: native

server:
  port: 8888
spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
#spring:
#  cloud:
#    config:
#      name: CloudConfigServierTestApplication
#      server:
#        git:
#          uri: https://github.com/MerryGaming/test-server

